package kt.date.utils.constants

object ErrorMessages {
    const val REQUIRED_OUTPUT_TYPE = "output type is required"
    const val REQUIRED_INPUT_PATTERN = "input pattern is required"

    const val OUTPUT_IS_EQUAL_TO_INPUT = "input and output are the same type"

    const val FAILED_TO_CONVERT_FOR_PATTERN = "failed to convert value for selected pattern"

    const val NOT_ACCEPTABLE_INPUT_PATTERN = "input is not following an acceptable pattern"
}